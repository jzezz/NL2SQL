services:
  - type: web
    name: nl2sql-vanna-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python scripts/setup_database.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GOOGLE_API_KEY
        sync: false